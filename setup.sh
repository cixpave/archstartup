{
  "name": "cachyos-setup-wizard",
  "version": "1.0.0",
  "description": "A browser-based, parallax setup wizard for a fresh CachyOS (Arch-based) machine — pick your apps, tips and optimizations, then install live or download a tailored script.",
  "bin": { "cachyos-wizard": "server.js" },
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=16" },
  "license": "MIT"
}
